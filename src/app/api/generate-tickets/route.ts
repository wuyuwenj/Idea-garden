import { NextResponse } from "next/server";

// Placeholder — ticket generation removed in simplified Seed model
export async function POST() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
